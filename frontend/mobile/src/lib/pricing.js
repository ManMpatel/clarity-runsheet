// Mirrors frontend/web/src/lib/pricing.js — no shared package between web and mobile, so this
// stays a manual copy. Keep both in sync when the tier prices change.
export const TIER_PRICING = { entry: 9, mid: 25, top: 45 }
export const TIER_LABELS = { entry: 'Entry', mid: 'Mid', top: 'Top' }
